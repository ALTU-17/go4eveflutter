import 'package:flutter/material.dart';


class Bookings extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(
    appBar: AppBar(
      title: Text('Bookings'),
    ),
    body: Center(child: Text('Bookings',style: TextStyle(fontSize: 60))),
  );
  
}
